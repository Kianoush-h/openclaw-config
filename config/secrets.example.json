{
  "_comment": "Copy to ~/.openclaw/secrets.json (chmod 600). Referenced from openclaw.json via SecretRef objects of the form { source: 'file', provider: 'filemain', id: '/path/to/key' }. The 'id' path maps into this JSON tree: '/slack/botToken' -> secrets.slack.botToken. NEVER commit the real file.",

  "gateway": {
    "authToken": "REPLACE_64_HEX_TOKEN_openssl_rand_-hex_32"
  },
  "slack": {
    "botToken": "xoxb-REPLACE-slack-bot-token",
    "appToken": "xapp-REPLACE-slack-app-level-token"
  },
  "brave": {
    "apiKey": "REPLACE-brave-search-api-key"
  },
  "hooks": {
    "token": "REPLACE_64_HEX_TOKEN_for_inbound_webhooks"
  },
  "vexa": {
    "apiKey": "REPLACE-vexa-mcp-x-api-key"
  }
}
