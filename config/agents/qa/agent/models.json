{
  "_comment": "Per-agent model catalog OVERRIDE. This file is OPTIONAL. If present it MUST contain a top-level 'providers' object — an empty '{}' is INVALID and triggers 'Invalid models.json schema: providers must have required properties providers'. If an agent has no special models, DELETE this file and let it inherit models from the root openclaw.json. Example of a valid minimal override below.",
  "providers": {
    "openrouter": {
      "baseUrl": "https://openrouter.ai/api/v1",
      "auth": "api-key",
      "api": "openai-completions",
      "apiKey": { "source": "env", "provider": "default", "id": "OPENROUTER_API_KEY" },
      "models": [
        { "id": "google/gemini-3.1-flash-lite", "name": "Gemini 3.1 Flash Lite", "contextWindow": 1048576 },
        { "id": "x-ai/grok-4.3", "name": "Grok 4.3", "contextWindow": 1000000 }
      ]
    }
  }
}
